query GetAllCategories {
  collections(first: 250) {
    nodes {
      id
      title
      handle
      image {
        url
        altText
      }
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
